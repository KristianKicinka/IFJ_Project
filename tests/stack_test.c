#include "../stack.h"
#include "../stack.c"
#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>



int main()
{/*
    Stack_symbol_t stack;
    printf("STACK TEST\n");
    Stack_Init(&stack);
    for(int i=0, i<100, i++){
        int c=Stack_Push(&stack, i, 'N');
        if(c==FALSE){
             printf("Malloc vratil false\n");
             break;
        }
    }
    
    for (int f = 0; f < 100; i++){
        char symbol=stack.top->stack_symbol;
    }
    */
  
    
    return 0;
}
